#include <iostream>
#include <vector>

using namespace std;

int swap(vector<int>&arr){
	int 
}

int main(){
	int n;
	cin << n;
	vector<int> arr(n);
	for(int i = 0; i < n; i++) cin<< arr[i];
	int result  = swap(arr);
	return 0;
}
